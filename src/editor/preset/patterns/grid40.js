import _gridPattern from "./_gridPattern"

export default { 
    key: 'grid40', 
    color: '#E7393F',
    backgroundColor: '#FFCDDF',
    execute: function () {
        return _gridPattern(40);
    }
}
