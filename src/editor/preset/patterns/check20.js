import _checkPattern from "./_checkPattern"

export default { 
    key: 'check20', 
    color: 'black',
    backgroundColor: 'white',
    execute: function () {
        return _checkPattern(20);
    }
}
