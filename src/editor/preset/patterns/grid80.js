import _gridPattern from "./_gridPattern"

export default { 
    key: 'grid80', 
    color: '#102C45',
    backgroundColor: 'gray',
    execute: function () {
        return _gridPattern(80);
    }
}
