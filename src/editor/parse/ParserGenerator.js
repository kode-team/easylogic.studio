export default class ParserGenerator {
  constructor(target, container) {
    this.target = target;
    this.container = container;

    this.initialize();
  }

  initialize() {}

  setSource(source) {
    this.source = source;
  }

  parse() {}

  generate() {}
}
